<!DOCTYPE html>
<html lang="ja">
    <head>
        <meta charset="UTF-8">
        <title>スケジュール編集フォーム：<%= user[0].title %></title>
        <link type="text/css" rel="stylesheet" href="/css/style.css">
    </head>
    <body>
        <section class="edit">
            <h1>編集フォーム</h1>
            <form class="editForm" action="/update/<%= user[0].id %>" method="POST">
                <div>
                    <label for="title">タイトル：</label><br>
                    <input type="text" id="title" name="title" value="<%= user[0].title %>" pattern="^[ぁ-んァ-ヶー-龥々a-z0-9\-]+$" maxlength="15" placeholder="ひらがな・カナ・漢字・英数字のみ" required>
                </div>
                <div>
                    <label for="name">記入者：</label><br>
                    <input type="text" id="name" name="name" value="<%= user[0].name %>" pattern="^[ぁ-んァ-ヶー-龥々a-z\-]+$" maxlength="10" placeholder="ひらがな・カナ・漢字・英字のみ" required>
                </div>
                <div>
                    <label for="content">スケジュール内容：</label><br>
                    <textarea id="content" name="content" cols="36" rows="6" maxlength="100" required><%= user[0].content %></textarea>
                </div>
                <div class="btns">
                    <span class="button"><button class="submitBtn" type="submit">更新</button></span>
                    <span class="button"><button class="deleteBtn"><a href="/delete/<%= user[0].id %>">削除</a></button></span>
                </div>
            </form>
        </section>
    </body>
</html>
